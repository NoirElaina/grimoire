# 工具调用模板

这个模板用来记录某个 Agent 如何接工具、怎么传参、失败时怎么兜底。

## 工具信息

- 工具名称：
- 用途：
- 输入参数：
- 输出结构：
- 超时策略：

## 调用前判断

- 什么时候必须调用：
- 什么时候尽量不调用：
- 是否可以并行调用：
- 是否有副作用：

## 调用规范

```json
{
  "tool": "tool_name",
  "reason": "为什么要调用",
  "input": {
    "fieldA": "value",
    "fieldB": "value"
  }
}
```

## 错误处理

- 参数不合法时：
- 工具超时时：
- 返回空结果时：
- 返回结果与预期冲突时：

## 记录建议

- 保留一次成功调用样例
- 保留一次失败调用样例
- 写清楚哪些字段是最容易错的
